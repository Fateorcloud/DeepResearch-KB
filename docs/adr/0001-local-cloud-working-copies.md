# Local And Cloud Working Copies

Local Control must work independently against a local `KnowledgeStore`; adding an optional cloud endpoint and CLI token enables explicit push, pull, and backup operations. We reject implicit real-time dual-master merging because version governance and conflict resolution must remain visible and user-controlled.
